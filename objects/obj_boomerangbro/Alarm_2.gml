/// @description Throw up a boomerang

if (throughsolid == 0)
&& (collision_rectangle(bbox_left,bbox_bottom+1,bbox_right,bbox_bottom+1,obj_semisolid,1,1)) {

    //Throw a hammer.
    alarm[3] = 30;
    
    //Set the throwing pose.
    shoot = 1;
}
else
    alarm[2] = 1;

